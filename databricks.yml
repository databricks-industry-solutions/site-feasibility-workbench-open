bundle:
  name: public-site-workbench

variables:
  app_name:
    description: "Databricks App name"
    default: "public-site-workbench"

targets:
  dev:
    default: true
    mode: development

  prod:
    mode: production

resources:
  apps:
    public-site-workbench:
      name: ${var.app_name}
      description: "Clinical Trial Site Feasibility Workbench"
      source_code_path: .
